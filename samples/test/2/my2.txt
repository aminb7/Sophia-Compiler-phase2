class mamd {
    s: int;
    a: string;
    orders: list(4 # list(name: string, price: int, quantity: int));
    m: list(a: int, c: string, int, bool);
    def mamd(current: int) {
        zz: func<string, int->void>;
        current = order.price * order.quantity + order.quantity * 100 % 1000 - order.price / 100 && mamad.c.x()[12].e() || c ;
    }

}